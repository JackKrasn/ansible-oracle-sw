#!/usr/bin/expect -f
spawn {{ oracle_home_db }}/OPatch/ocm/bin/emocmrsp -no_banner -output {{ patch_dir }}/{{ oracle_version }}/ocm.rsp
expect {
  "Email address/User Name:"
  {
    send "\r"
    exp_continue
  }
  "Do you wish to remain uninformed of security issues*"
  {
    send "Y\r"
    exp_continue
  }
}

